// useAudio — Phase 4
// Manages getUserMedia, WebRTC peer connections, and local mute state.
export function useAudio() {
  // TODO: implement in Phase 4
  return { muted: true, toggleMute: () => {} };
}
