// useWebSocket — Phase 3
// Manages WebSocket connection lifecycle, reconnection, and event routing.
export function useWebSocket(_roomId: string) {
  // TODO: implement in Phase 3
  return { send: (_msg: unknown) => {} };
}
